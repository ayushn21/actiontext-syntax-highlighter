module ActionTextSyntaxHighlighter
  class HighlightedCodeBlockRenderingTest < ActiveSupport::TestCase
    test "tries to guess language if it's missing" do

    end

    test "language returns empty string if nil" do
    end

    test "removes br tags before saving" do
    end

    test "blocks without a rich text and older than one day are considered deleted" do
    end

    test "blocks without a rich text and less than a day old aren't considered deleted" do
    end

    test "supported languages contains an empty string as the first element" do
    end
  end
end