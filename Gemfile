source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in actiontext-syntax-highlighter.gemspec.
gemspec

group :development do
  gem 'sqlite3'
end

group :development, :test do
  gem 'byebug'
end
